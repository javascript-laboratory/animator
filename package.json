{
  "name": "animator",
  "version": "0.0.1",
  "description": "A class for managing and controlling browser animations.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.6",
    "typescript": "^4.2.4"
  },
  "jest": {
    "preset": "ts-jest",
    "clearMocks": true
  }
}
